{
  "name": "tesseract",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "nodemon": "npx nodemon --exitcrash",
    "dev": "npx ts-node --transpile-only ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "noop": "tail -f /dev/null"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/client": "^3.8.7",
    "node-fetch": "^3.3.2",
    "redis-smq": "^7.0.7",
    "tesseract.js": "^5.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
