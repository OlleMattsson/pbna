/**
 * Shared Prettier configuration for the React Admin frontend.
 * Keep it in sync with any editor integrations so on-save formatting matches CLI output.
 */
module.exports = {
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: true,
  trailingComma: 'es5',
  bracketSpacing: true,
  arrowParens: 'always',
};
